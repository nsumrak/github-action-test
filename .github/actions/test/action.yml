# .github/actions/test/action.yml

name: "Test Workflow"
description: "Test workflow."

runs:
  using: "composite"
  steps:

    - name: log
      shell: bash
      run: |
        set -o xtrace
        echo "Action test"
